generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite" // oder postgres, mysql etc.
  url      = "file:./dev.db"
}

model Book {
  id              Int    @id @default(autoincrement())
  title           String
  author          String
  publicationYear Int
}